/*
function* range(_start_, _end_) {
  for (let h = _start_; h <= _end_; h++) {
    yield h;
    }
    }

btnFindRange.onclick= for (h of range(inptStartNumber, inptEndNumber)) {
  lblRangeOutput.value = h;
    }
    
    


*/